[project]
name = "sentinel-audit"
version = "0.1.0"
description = "Agentic Auditing"
readme = "README.md"
requires-python = ">=3.12.12"
dependencies = [
    "groq>=1.0.0",
    "mcp[cli]>=1.26.0",
    "python-dotenv>=1.2.1",
]
